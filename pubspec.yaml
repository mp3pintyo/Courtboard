name: courtboard

description: Sportolókövető asztali alkalmazás Flutterhez.
publish_to: none
version: 0.8.0+8

environment:
  sdk: '>=3.12.0 <4.0.0'
  flutter: '>=3.44.0'

dependencies:
  flutter:
    sdk: flutter
  html: ^0.15.6
  sqflite_common_ffi: ^2.4.2
  xml: ^6.6.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
